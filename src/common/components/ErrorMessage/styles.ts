import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme) => ({
  text: {
    textAlign: "center",
    fontWeight: 700,
    fontSize: "1rem",
    color: "#CD2727",
  },
}));

export default useStyles;
